# @vividcodeai/embedded-serial-mcp-darwin-x64

适用于 macOS x86_64 的预编译二进制文件。这是 `@vividcodeai/embedded-serial-mcp` 的平台依赖包。

**不要直接安装此包。** 请安装父包：

```bash
npm install @vividcodeai/embedded-serial-mcp
```

---

[English Documentation](README.md)
